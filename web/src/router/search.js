import GlobalSearch from '@/views/search/GlobalSearch.vue'

const routes = [
    {
        path: '/search/global',
        component: GlobalSearch,
        meta: { sTitle: '统一搜索', tTitle: '全局搜索' }
    }
]

export default routes
