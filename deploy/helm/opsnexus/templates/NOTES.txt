{{- if .Values.ingress.enabled }}
OpsNexus is available via ingress:
{{- range .Values.ingress.hosts }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if .Values.web.enabled }}
OpsNexus web service:
  kubectl port-forward svc/{{ include "opsnexus.componentFullname" (dict "root" . "component" "web") }} 8080:{{ .Values.web.service.port }}

OpsNexus API service:
  kubectl port-forward svc/{{ include "opsnexus.componentFullname" (dict "root" . "component" "api") }} 8000:{{ .Values.api.service.port }}
{{- else }}
OpsNexus chart installed. The web component is disabled, so expose the API manually if needed.
{{- end }}

