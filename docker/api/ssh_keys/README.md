Place SSH key material here only on your local machine.

This directory is intentionally kept in the repository so Docker bind mounts
continue to work, but real key files are ignored and must not be committed.
